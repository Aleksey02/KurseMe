version: '3.8'

services:
  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "80:80"
    restart: always
    networks:
      - my_network

networks:
  my_network:
    external: true